# Test fixture workspace

This folder is opened as the workspace when running the extension test suite
(`npm test`). It contains a sample `.gitignore` so that `explorer.excludeGitIgnore`
has something meaningful to act on.
